# =============================================================================
# HANBALI HERITAGE ACADEMY - ENVIRONMENT VARIABLES
# =============================================================================

# -----------------------------------------------------------------------------
# CONVEX DATABASE & BACKEND
# -----------------------------------------------------------------------------
# Convex deployment ID (auto-generated when you run `npx convex dev`)
CONVEX_DEPLOYMENT=dev:your-deployment-name

# Public Convex URL (auto-generated, used by frontend and scripts)
NEXT_PUBLIC_CONVEX_URL=https://your-deployment-name.convex.cloud

# -----------------------------------------------------------------------------
# AUTHENTICATION & SECURITY
# -----------------------------------------------------------------------------
# JWT Secret for token signing (CHANGE THIS IN PRODUCTION!)
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production

# -----------------------------------------------------------------------------
# EMAIL SERVICE (OPTIONAL - FOR PRODUCTION OTP DELIVERY)
# -----------------------------------------------------------------------------
# Resend API Key for sending OTP emails in production
# Get your key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key

# -----------------------------------------------------------------------------
# ENVIRONMENT
# -----------------------------------------------------------------------------
# Node environment (automatically set by Next.js)
# NODE_ENV=development|production

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env.local
# 2. Run `npx convex dev` to generate CONVEX_DEPLOYMENT and NEXT_PUBLIC_CONVEX_URL
# 3. Generate a strong JWT_SECRET for production
# 4. (Optional) Add RESEND_API_KEY for production email delivery
# ===========================